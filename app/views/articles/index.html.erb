<div class="subcategory">
  <% @categories.each do |category| %>
    <%= link_to category.name, articles_path(:cate => category.id) %>
    <% category.subcategories.each do |subcategory| %>
      <% link_to subcategory.name, articles_path(subcategory: subcategory.name) %>
    <% end %>
  <% end %>
</div>

<div class="subcategory">
  <% @subcategories.each do |subcategory| %>
    <%= link_to subcategory.name, articles_path(:subcate => subcategory.id) %>
  <% end %>
</div>

<!-- <div class="subcategory">
    <%= link_to "Bien-être", articles_path(:cate => 4) %>
    <%= link_to "Recettes", articles_path(:cate => 1) %>
    <%= link_to "Santé", articles_path(:cate => 5) %>
    <%= link_to "Santé au féminin", articles_path(:cate => 7) %>
    <%= link_to "Sport", articles_path(:cate => 8) %>
    <%= link_to "Lifestyle", articles_path(:cate => 6) %>
</div>   -->

<%# <div class="subcategory">
<%#   <% @subcategories.each do |subcategory| %>
<%#     <% if subcategory.id = 2 %>
<%#       <%= link_to subcategory.name %>
<%#     <% end %>
<%#   <% end %>
<%# </div>    %>

<% @article.each do |article| %>
  <div class="card-article">
     <%= cl_image_tag article.photo.key, width: 500 %>
     <div class="card-article-infos">
        <div>
          <h2><%= article.title %></h2>
        </div>
        <div>
          <p><%= article.description %></p>
        </div>
        <div class="links">
          <% if policy(article).edit? %>
          <%= link_to 'Mettre à jour', edit_article_path(article), class: "btn btn-ghost" %>
          <% end %>
          <% if policy(article).destroy? %>
           <%= link_to 'Supprimer', article_path(article), method: :delete, data: {confirm: "Êtes-vous sûr ?"}, class: "btn btn-ghost" %>
          <% end %>
          <%= link_to 'Lire', article_path(article), class: "btn btn-ghost" %>
        </div>
     </div>
  </div>
<% end %>


<% if policy(Article.new).create? %>
  <%= link_to 'Nouvel article', new_article_path, class: "btn btn-ghost" %>
<% end %>
